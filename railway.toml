# Railway build/deploy config.
# Railway auto-injects PORT (the server already honors process.env.PORT),
# provides HTTPS on the generated domain, and stays always-on.

[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
